<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="SIGEBI.Desktop.Presentacion.Views.Recursos.RecursosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#F0F2F5">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="+ Agregar Libro" Style="{StaticResource PrimaryButton}" Click="BtnAgregarLibro_Click" Margin="0,0,8,0"/>
            <Button Content="+ Agregar Revista" Style="{StaticResource SuccessButton}" Click="BtnAgregarRevista_Click" Margin="0,0,8,0"/>
            <Button Content="Actualizar" Style="{StaticResource PrimaryButton}" Click="BtnActualizar_Click"/>
        </StackPanel>
        <TextBox x:Name="TxtBusqueda" Grid.Row="1" Style="{StaticResource ModernTextBox}" Width="300" HorizontalAlignment="Left" Margin="0,0,0,16" TextChanged="TxtBusqueda_TextChanged"/>
        <Border Grid.Row="2" CornerRadius="8" ClipToBounds="True" BorderBrush="#DEE2E6" BorderThickness="1">
            <DataGrid x:Name="DgRecursos">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Codigo" Binding="{Binding Codigo}" Width="100"/>
                    <DataGridTextColumn Header="Titulo" Binding="{Binding Titulo}" Width="*"/>
                    <DataGridTextColumn Header="Autor" Binding="{Binding Autor}" Width="180"/>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="80"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="70"/>
                    <DataGridTextColumn Header="Disponible" Binding="{Binding Disponible}" Width="90"/>
                    <DataGridTemplateColumn Header="Acciones" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="+ Stock" Tag="{Binding Id}" Click="BtnIncrementar_Click" Background="#28A745" Foreground="White" BorderThickness="0" Padding="8,4" FontSize="11" Cursor="Hand" Margin="0,0,4,0"/>
                                    <Button Content="- Stock" Tag="{Binding Id}" Click="BtnReducir_Click" Background="#DC3545" Foreground="White" BorderThickness="0" Padding="8,4" FontSize="11" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>